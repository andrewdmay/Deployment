resources:
  containers:
    - container: dotnetsdk
      image: microsoft/dotnet:2.1.503-sdk-stretch
    - container: dind
      image: docker:latest

jobs:
  - job: set_version
    pool: AWS Ubuntu
    container: dotnetsdk
    steps:
      - script: |
          export PATH="$PATH:$HOME/.dotnet/tools"
          dotnet tool install -g nbgv
          nbgv cloud -a
      - bash: echo '##vso[task.setvariable variable=nbgv_cloudbuildnumber;isOutput=true]$(NBGV_CloudBuildNumber)'
        name: set_variable
  - job: echo_vars
    dependsOn: set_version
    pool: AWS Ubuntu
    variables:
      nbgv_cloudbuildnumber: $[ dependencies.set_version.outputs['set_variable.nbgv_cloudbuildnumber'] ]
    steps:
      - bash: echo $NBGV_CLOUDBUILDNUMBER
      - bash: echo $(nbgv_cloudbuildnumber)
  - job: task_build
    dependsOn: set_version
    pool: AWS Ubuntu
    container: dotnetsdk
    steps:
      - task: DotNetCoreCLI@2
        displayName: Dotnet Build
        inputs:
          command: build
          projects: '**/*.csproj'
      - task: DotNetCoreCLI@2
        displayName: Dotnet Test
        inputs:
          command: test
          projects: '**/*Tests.csproj'
  - job: docker_build
    dependsOn: set_version
    pool: AWS Ubuntu
    variables:
      nbgv_cloudbuildnumber: $[ dependencies.set_version.outputs['set_variable.nbgv_cloudbuildnumber'] ]
    steps:
      - task: Docker@1
        displayName: Docker Build
        inputs:
          command: build
          imageName: deployment:$(nbgv_cloudbuildnumber)
  - job: cake_build
    pool: AWS Ubuntu
    dependsOn: set_version
    container: dotnetsdk
    steps:
      - script: |
          export PATH="$PATH:$HOME/.dotnet/tools"
          dotnet tool install -g Cake.Tool --version 0.32.0
          dotnet cake --bootstrap
          dotnet cake
